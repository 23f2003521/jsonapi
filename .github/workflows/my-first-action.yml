name: Assignment Action

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: 23f2003521@ds.study.iitm.ac.in  # Your required step
        run: echo "Hello from my GitHub Action!"
